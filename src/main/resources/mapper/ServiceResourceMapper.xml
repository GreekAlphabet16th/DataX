<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cetiti.dataX.dao.ServiceResourceDao">
    <select id="getServiceResource" resultType="ServiceResource">
      SELECT resource_url, data_id FROM SERVICE_RESOURCE  LEFT JOIN API_INFO ON SERVICE_RESOURCE.category_id = #{categoryId}
    </select>
</mapper>
